<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:id="@+id/root"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/contactImage"
        android:layout_width="@dimen/contact_image_size"
        android:layout_height="@dimen/contact_image_size"
        android:layout_margin="20dp"
        tools:background="@color/toolbar_color" />

    <android.support.v7.widget.AppCompatCheckBox
        android:id="@+id/contactSelected"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:layout_marginEnd="20dp"
        android:layout_marginRight="20dp"
        android:layout_marginTop="20dp"
        />

    <TextView
        android:id="@+id/contactText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="@dimen/default_padding"
        android:layout_marginStart="@dimen/default_padding"
        android:layout_marginTop="20dp"
        android:layout_toEndOf="@id/contactImage"
        android:layout_toLeftOf="@id/contactSelected"
        android:layout_toRightOf="@id/contactImage"
        android:layout_toStartOf="@id/contactSelected"
        android:fontFamily="sans-serif"
        android:gravity="center_vertical"
        android:lineSpacingExtra="8sp"
        android:maxLines="2"
        android:minHeight="48dp"
        android:textColor="@android:color/black"
        android:textSize="16sp"
        tools:text="Инокентий Виссарионович Кириллов" />

</RelativeLayout>